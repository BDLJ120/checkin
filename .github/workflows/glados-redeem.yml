
    #- cron: '*/5 23 * * *'  # 每5分钟运行一次，UTC时间23点（对应北京时间7点）
    #- cron: '*/5 0-3 * * *'  # 每5分钟运行一次，UTC时间0-3点（对应北京时间8-12点）
    #- cron: '*/5 23,0-2 * * *'  # 每5分钟运行一次，UTC时间0-3点（对应北京时间8-12点）
    #- cron: '30,35,40,45,50,55 3 * * *'
name: GLaDOS Auto Redeem

on:
  schedule:
    #北京时间 7 点到 11 点每半小时运行一次，
    - cron: '0,30 0-2 * * *'
  workflow_dispatch:  # 保留手动触发，方便测试

jobs:
  redeem:
    runs-on: ubuntu-latest
    # 声明北京时间，日志时间更直观
    env:
      TZ: Asia/Shanghai
    steps:
      # 修复之前重复的语法错误
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4
      - name: Run redeem script
        id: run-redeemrun-redeem
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GLADOS_COOKIES: ${{ secrets.GLADOS_COOKIES }}
        run: |
          ls -la
          python .github/workflows/glados_redeem.py
